# Laisser vide pour utiliser des URLs relatives (recommandé en Docker/production)
# VITE_API_URL=
# Ou spécifier l'URL complète pour un accès direct (dev uniquement)
# VITE_API_URL=http://localhost:3001/api
